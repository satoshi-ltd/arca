`gallery.heic` is an Arca-generated test image: an 80 × 60 solid #dd6633
rectangle made with Sharp and encoded using macOS `sips -s format heic`.
It contains no user photograph or personal metadata. Tests decode the committed
fixture on every platform; generating it is not a test prerequisite.
